# EnTeX session nav — local control plane (not part of product charter)
version: 1
updated: "2026-09-12"
scope: project

session:
  intent: "IR vocabulary doc + first doc-package scaffold (schema.json, 12 IR examples) on branch 5/npc/ir_type_vocabulary, uncommitted; A案 for derived values agreed"
  turn_estimate: 5
  chat_id_hint: "entex-ir-types"

navigation:
  active_thread_id: "ir-type-vocabulary"
  active_plan: ""
  next_actions:
    - "NPC: first commit 540a48d is in. Commit this turn as `docs:月次報告書のIR例とスキーマを書く`; create issue, rename branch if number != 5; open PR; post `@coderabbitai review`"
    - "Obtain the real circle monthly report (Word) and fix packages/circle-monthly-report/schema.json fields"
    - "Roadmap step 1 can start from examples/valid/01-typical.json: pydantic loader for schema.json (案1) + envelope + expr evaluator + escape, then template.tex.j2"
    - "User decides P0 gate (proposal: Go). Then 要求 for first doc-type: サークル月次報告書"
    - "Roadmap step 1: `entex render <json>` with first doc-package under packages/ (CLI JSON→PDF)"
    - "WSL clone ~/EnTeX has no GitHub SSH key — add key or use credential helper before pushing from WSL"
    - "From now on: no direct commits on main — branch `issue/owner/slug`, commit `prefix:text`, PR into main (AGENTS.md)"
    - "Tell NPC about the new branch/commit/PR rules and the CodeRabbit habit (`@coderabbitai review`)"
  blockers: []

alignment:
  project_state_path: docs/project-state.yaml
  phase: P0
  scope_level: H0
  gate_status: pending

pressure:
  level: low
  signals: []
  last_l2_flush_at: ""
  reanchor_required: false
  pre_compact_recommended: false

verification:
  last_verified_at: "2026-09-12"
  last_nav_brief_at: "2026-09-12"
  chat_vs_file_delta: none
