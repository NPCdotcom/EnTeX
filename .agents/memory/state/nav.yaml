# EnTeX session nav — local control plane (not part of product charter)
version: 1
updated: "2026-09-12"
scope: project

session:
  intent: "P0 gate = Go (user, 2026-09-12). 要求.md(P1) → 要件.md(P2) → renderer.md(P3, H4) all written. User explicitly asked to STOP at design: plan-record(P4) and implement-conduct(P5) are deferred to a separate agent/session."
  turn_estimate: 5
  chat_id_hint: "entex-ir-types"

navigation:
  active_thread_id: "ir-type-vocabulary"
  active_plan: ""
  next_actions:
    - "STOP HERE for this agent/session: do not run plan-record or implement-conduct unless the user explicitly re-authorizes it in this session"
    - "Handoff for the next (implementation) agent: read docs/design/programs/renderer.md 次節— plan-record two plans: `ir-validate-and-derive` (H5, FR1-4, TeX-independent, testable via `make test`) and `render-and-cli` (H4, FR5-8, needs `make docker-test`)"
    - "Handoff note: plan-record still needs PM user confirmation before status:agreed (PROJECT_LIFECYCLE.md gate table), even though the design condition (H4 parent linkable) is now met"
    - "Obtain the real circle monthly report (Word) to fix packages/circle-monthly-report/schema.json and template.tex.j2 later — does not block plan-record/implementation"
    - "NPC: open PR for 5/npc/ir_type_vocabulary manually (no gh CLI on this host); post `@coderabbitai review` on new pushes"
    - "WSL clone ~/EnTeX has no GitHub SSH key — add key or use credential helper before pushing from WSL"
  blockers: []

alignment:
  project_state_path: docs/project-state.yaml
  phase: P3
  scope_level: H4
  gate_status: passed

pressure:
  level: low
  signals: []
  last_l2_flush_at: ""
  reanchor_required: false
  pre_compact_recommended: false

verification:
  last_verified_at: "2026-09-12"
  last_nav_brief_at: "2026-09-12"
  chat_vs_file_delta: gate_recorded
