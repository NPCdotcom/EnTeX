# EnTeX session nav — local control plane (not part of product charter)
version: 1
updated: "2026-09-12"
scope: project

session:
  intent: "User re-authorized implementation ('P3の設計に従って実装に入ってください'). plan-record (P4) for ir-validate-and-derive + render-and-cli, then implement-conduct (P5) for both. Roadmap step 1 (CLI JSON → PDF) now works end-to-end."
  turn_estimate: 1
  chat_id_hint: "entex-renderer-impl"

navigation:
  active_thread_id: "renderer-implementation"
  active_plan: ".agents/plans/programs/render-and-cli.md"
  next_actions:
    - "P6: review-conduct on the implementation PR (branch cursor/renderer-cli-render-172b). Check V-model: AC ↔ tests ↔ 要件.md FR1–FR8/NFR1–2 ↔ renderer.md"
    - "CI: confirm the tex job (docker) passes incl. NFR1 timing test; if the first-run font cache makes it flaky, raise ENTEX_NFR1_SECONDS or warm differently"
    - "Branch name follows the cloud-agent template (cursor/…), not AGENTS.md's issue/owner/topic — rename or accept when merging; open an issue number for this work"
    - "Obtain the real circle monthly report (Word) → adjust packages/circle-monthly-report/schema.json + template.tex.j2 + style (changes must stay inside the package)"
    - "Next roadmap step (charter §11 step 2): API — reuse the same 4-step pipeline (load_and_validate → apply_derived → render). Consider a shared pipeline function before adding FastAPI"
    - "Post `@coderabbitai review` on the PR after opening / after pushes (AGENTS.md)"
  blockers: []

alignment:
  project_state_path: docs/project-state.yaml
  phase: P5
  scope_level: H4
  gate_status: passed

pressure:
  level: low
  signals: []
  last_l2_flush_at: ""
  reanchor_required: false
  pre_compact_recommended: false

verification:
  last_verified_at: "2026-09-12"
  last_nav_brief_at: "2026-09-12"
  chat_vs_file_delta: none
