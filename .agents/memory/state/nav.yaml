# EnTeX session nav — local control plane (not part of product charter)
version: 1
updated: "2026-09-12"
scope: project

session:
  intent: "P0 gate = Go (user, 2026-09-12). Agreed renderer-first order among the 6 主要要素. Writing P1 要求 (circle-monthly-report) now; plan-record for renderer before any implement-conduct."
  turn_estimate: 5
  chat_id_hint: "entex-ir-types"

navigation:
  active_thread_id: "ir-type-vocabulary"
  active_plan: ""
  next_actions:
    - "docs/requirements/circle-monthly-report/要求.md written (P1) — user review, then 要件.md (P2)"
    - "Then 要件.md (P2, verifiable acceptance) and basic design docs/design/programs/renderer.md (P3, H4) before plan-record (P4)"
    - "renderer build order once plan is agreed: pydantic loader for schema.json (案1) → envelope check → expr evaluator (derived) → TeX escape → template.tex.j2 (provisional layout ok) → latexmk wrapper; target examples/valid/01-typical.json first"
    - "Obtain the real circle monthly report (Word) to fix packages/circle-monthly-report/schema.json and template.tex.j2 later — does not block steps 1-4 above"
    - "NPC: open PR for 5/npc/ir_type_vocabulary manually (no gh CLI on this host); post `@coderabbitai review`"
    - "WSL clone ~/EnTeX has no GitHub SSH key — add key or use credential helper before pushing from WSL"
  blockers: []

alignment:
  project_state_path: docs/project-state.yaml
  phase: P1
  scope_level: H1
  gate_status: passed

pressure:
  level: low
  signals: []
  last_l2_flush_at: ""
  reanchor_required: false
  pre_compact_recommended: false

verification:
  last_verified_at: "2026-09-12"
  last_nav_brief_at: "2026-09-12"
  chat_vs_file_delta: gate_recorded
