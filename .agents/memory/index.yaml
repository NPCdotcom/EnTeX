# EnTeX project index — entries the agent kit recalls. Keep in sync with the files it points at.
# Template: .agents/memory/index.yaml.example

version: 1
updated: "2026-09-12"
scope: project

priority_order: [index, state, episodes, traces, evaluations, knowledge, history, rewards]

read_budget:
  anchor_max: 5
  thread_max: 3
  episode_latest_max: 2

# Global layer: separate index at ~/.agents/memory/global/index.yaml
# See docs/MEMORY_ARCHITECTURE.md

entries:
  - id: nav-control-plane
    scope: project
    tier: state
    granularity: anchor
    path: .agents/memory/state/nav.yaml
    summary: "Session nav — intent, next_actions, pressure"
    tags: [nav, control-plane, every_turn]
    recall: every_turn
    linked: [project-state]

  - id: project-state
    scope: team
    tier: state
    granularity: anchor
    path: docs/project-state.yaml
    summary: "Phase, H scope, gate_status, blockers"
    tags: [lifecycle, gate, canonical]
    recall: every_turn
    linked: []

  - id: ep-ir-type-vocabulary
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-ir-type-vocabulary.md
    summary: "IR type vocabulary reviewed; design doc at docs/design/elements/ir-type-vocabulary.md"
    tags: [ir-schema, H1, design]
    recall: on_demand
    linked: [project-state]

  - id: audit-p0-go
    scope: team
    tier: audit
    granularity: anchor
    path: .agents/memory/audit/audit-log.md
    summary: "P0→P1 gate: user Go (2026-09-12); renderer-first order agreed among 6 主要要素"
    tags: [gate, P0, P1, renderer]
    recall: on_demand
    linked: [project-state]

  - id: req-circle-monthly-report
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/requirements/circle-monthly-report/要求.md
    summary: "P1 要求 for the first doc-type (サークル月次報告書), grounded in charter §3/§5 and packages/circle-monthly-report/README.md. SUPERSEDED as production target by ADR-0001 (2026-09-12, issue #11) — kept as reference; new doc-type is 部会ログ"
    tags: [requirements, P1, circle-monthly-report, superseded]
    recall: on_demand
    linked: [project-state, adr-0001-meeting-log-pivot]

  - id: reqspec-circle-monthly-report
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/requirements/circle-monthly-report/要件.md
    summary: "P2 要件 — FR1-FR8 (envelope/type検証・derived計算・escape・latexmk・CLI) + NFR1-2 (10秒以内・決定性), open items for P3. Content reference only; doc-type itself superseded by ADR-0001"
    tags: [requirements, P2, circle-monthly-report, renderer, superseded]
    recall: on_demand
    linked: [project-state, req-circle-monthly-report, adr-0001-meeting-log-pivot]

  - id: adr-0001-meeting-log-pivot
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/adr/0001-first-doc-type-pivot-to-meeting-log.md
    summary: "ADR-0001 (2026-09-12, issue #11): pivot first doc-type from circle-monthly-report (operation doesn't actually exist) to formatting the circle's 部会ログ (meeting log). renderer/pipeline/CLI/API untouched; impact confined to packages/circle-monthly-report/ + its requirements docs. Concrete format pending user input"
    tags: [adr, decision, doc-type, pivot, issue-11]
    recall: on_demand
    linked: [project-state, req-circle-monthly-report, reqspec-circle-monthly-report, design-renderer]

  - id: design-renderer
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/design/programs/renderer.md
    summary: "P3 basic design (H4) for renderer — module split, interfaces, error mapping, provisional template layout policy. Open items resolved at P4/P5 (static pydantic + type walker; tabularx without longtable)."
    tags: [design, P3, H4, renderer]
    recall: on_demand
    linked: [project-state, reqspec-circle-monthly-report, plan-render-and-cli]

  - id: plan-ir-validate-and-derive
    scope: project
    tier: knowledge
    granularity: thread
    path: .agents/plans/algorithms/ir-validate-and-derive.md
    summary: "P4 plan (H5) FR1–FR4 — envelope/type validation + derived values. Do + Check complete 2026-09-12 (pass); AC1–5 covered by tests/test_ir_*.py"
    tags: [plan, P4, P5, H5, ir-schema]
    recall: on_demand
    linked: [design-renderer, project-state]

  - id: plan-render-and-cli
    scope: project
    tier: knowledge
    granularity: thread
    path: .agents/plans/programs/render-and-cli.md
    summary: "P4 plan (H4) FR5–FR8, NFR1–2 — escape, template, latexmk, CLI render. Do + Check complete 2026-09-12 (pass; W1 job name, W2 missing template → Act)"
    tags: [plan, P4, P5, H4, renderer, cli]
    recall: on_demand
    linked: [design-renderer, project-state]

  - id: ep-renderer-implementation
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-renderer-implementation.md
    summary: "plan-record + implementation of renderer (roadmap step 1). Module map, decisions, risks (branch name vs AGENTS.md, no Docker locally)"
    tags: [renderer, P5, implementation]
    recall: on_demand
    linked: [plan-render-and-cli, plan-ir-validate-and-derive]

  - id: review-renderer-cli-p6
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/reviews/2026-09-12-renderer-cli-p6-review.md
    summary: "P6 review of PR #8 (renderer/CLI): RTM FR1–8/NFR1–2 all pass; W1 latexmk job name from file stem, W2 missing template → exit 2; S1–S4 suggestions"
    tags: [review, P6, renderer, cli]
    recall: on_demand
    linked: [plan-render-and-cli, plan-ir-validate-and-derive, design-renderer]

  - id: design-api
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/design/programs/api.md
    summary: "P3 design (H4, agreed 2026-09-12) for roadmap step 2 — entex.pipeline shared by CLI/API, POST /v1/render → PDF, RFC 9457 Problem Details, sync def + semaphore, P4 split pipeline-and-cli / api-render"
    tags: [design, P3, H4, api, pipeline]
    recall: on_demand
    linked: [design-renderer, review-renderer-cli-p6, project-state, plan-pipeline-and-cli, plan-api-render]

  - id: plan-pipeline-and-cli
    scope: project
    tier: knowledge
    granularity: thread
    path: .agents/plans/programs/pipeline-and-cli.md
    summary: "P4 plan (H4, agreed; Do done + Check pass 2026-09-12, merged in PR #10) — entex.pipeline.render_ir/prepare, CLI on top, absorbs review W1 (job name) / W2 (missing template → PackageError) / S1 (renderer.md IF) / S3 (TEXINPUTS). TeX-independent"
    tags: [plan, P4, P5, P6, H4, pipeline, cli]
    recall: on_demand
    linked: [design-api, review-renderer-cli-p6, review-api-and-pipeline-p6, project-state, ep-p5-pipeline-and-cli]

  - id: plan-api-render
    scope: project
    tier: knowledge
    granularity: thread
    path: .agents/plans/programs/api-render.md
    summary: "P4 plan (H4, agreed; Do done + Check pass 2026-09-12, PR #12 ready for review) — FastAPI POST /v1/render → PDF, /v1/health, /v1/doc-types*, RFC 9457 problems, settings, schemas/api, Dockerfile CMD. Auth out of scope. Review W1: stream the body for 413 before public deploy"
    tags: [plan, P4, P5, P6, H4, api]
    recall: on_demand
    linked: [design-api, plan-pipeline-and-cli, review-api-and-pipeline-p6, project-state, ep-p5-api-render]

  - id: review-api-and-pipeline-p6
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/reviews/2026-09-12-api-and-pipeline-p6-review.md
    summary: "P6 review of pipeline-and-cli (PR #10) + api-render (PR #12): RTM FR-A1–A8 / NFR-A1–A4 + 10 plan ACs all pass, 217 tests, CI green; W1 chunked body buffered before 413 (stream before public deploy); S1–S6 (api.md §3.1 wording, RenderResult.title, internal-error test, is_valid_slug fullmatch, Settings tests, 4-concurrent NFR-A1)"
    tags: [review, P6, api, pipeline]
    recall: on_demand
    linked: [plan-api-render, plan-pipeline-and-cli, design-api, review-renderer-cli-p6, ep-p6-review-api-and-pipeline]

  - id: ep-p6-review-and-api-design
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-p6-review-and-api-design.md
    summary: "P6 review pass + API P3 design on branch feature/npc; W1/W2 deferred into pipeline plan; branch name vs AGENTS.md rule"
    tags: [review, api, P6, P3]
    recall: on_demand
    linked: [review-renderer-cli-p6, design-api]

  - id: ep-p5-pipeline-and-cli
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-p5-pipeline-and-cli.md
    summary: "P5 Do of pipeline-and-cli on feature/npc (PR #10): entex.pipeline new, CLI via pipeline only, W1/W2/S1/S3 fixed, 168 tests pass on TeX host; pct-hash (no trailing dash) deviation noted"
    tags: [implement, P5, pipeline, cli]
    recall: on_demand
    linked: [plan-pipeline-and-cli, design-api, design-renderer]

  - id: ep-p5-api-render
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-p5-api-render.md
    summary: "P5 Do of api-render on feature/npc (PR #10): entex.api (FastAPI, RFC 9457 Problem Details, semaphore, temp dirs), RenderTimeoutError, schemas/api, Dockerfile CMD uvicorn; 217 tests; uvicorn+curl manual check. User chose to Check both plans together"
    tags: [implement, P5, api, fastapi]
    recall: on_demand
    linked: [plan-api-render, plan-pipeline-and-cli, design-api]

  - id: ep-p6-review-api-and-pipeline
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-p6-review-api-and-pipeline.md
    summary: "Combined P6 Check of pipeline-and-cli + api-render: pass (0 Critical / 1 Warning / 6 Suggestion), no drive-by fixes; project-state → P6; PR #12 marked ready; next = merge (user) then roadmap step 3 P3 design"
    tags: [review, P6, api, pipeline]
    recall: on_demand
    linked: [review-api-and-pipeline-p6, plan-api-render, plan-pipeline-and-cli]

  - id: ep-charter-markdown-input-issue-14
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-charter-markdown-input-issue-14.md
    summary: "Charter revised for issue #14: 47 real 部会ログ census → input = front matter + Markdown with package-declared sections (§5.1), §6 体裁上同一 criteria, data-import first path, club-meeting-log new package replaces 1st doc-type, 着手順3 → 3a/3b. Follow-ups: ir-type-vocabulary document/image, ADR-0001 closure, TEAM.md split"
    tags: [charter, P0, H0, doc-type, markdown, issue-14]
    recall: on_demand
    linked: [adr-0001-meeting-log-pivot, ep-ir-type-vocabulary, project-state]

  # Optional pointer to global knowledge (no duplicate body):
  # - id: link-global-lessons
  #   scope: project
  #   tier: index
  #   granularity: archive
  #   path: null
  #   summary: "See global index: lesson-powershell-heredoc"
  #   tags: [shell]
  #   recall: on_demand
  #   linked: [lesson-powershell-heredoc]
