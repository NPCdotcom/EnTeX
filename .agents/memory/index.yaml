# EnTeX project index — entries the agent kit recalls. Keep in sync with the files it points at.
# Template: .agents/memory/index.yaml.example

version: 1
updated: "2026-09-12"
scope: project

priority_order: [index, state, episodes, traces, evaluations, knowledge, history, rewards]

read_budget:
  anchor_max: 5
  thread_max: 3
  episode_latest_max: 2

# Global layer: separate index at ~/.agents/memory/global/index.yaml
# See docs/MEMORY_ARCHITECTURE.md

entries:
  - id: nav-control-plane
    scope: project
    tier: state
    granularity: anchor
    path: .agents/memory/state/nav.yaml
    summary: "Session nav — intent, next_actions, pressure"
    tags: [nav, control-plane, every_turn]
    recall: every_turn
    linked: [project-state]

  - id: project-state
    scope: team
    tier: state
    granularity: anchor
    path: docs/project-state.yaml
    summary: "Phase, H scope, gate_status, blockers"
    tags: [lifecycle, gate, canonical]
    recall: every_turn
    linked: []

  - id: ep-ir-type-vocabulary
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-ir-type-vocabulary.md
    summary: "IR type vocabulary reviewed; design doc at docs/design/elements/ir-type-vocabulary.md"
    tags: [ir-schema, H1, design]
    recall: on_demand
    linked: [project-state]

  - id: audit-p0-go
    scope: team
    tier: audit
    granularity: anchor
    path: .agents/memory/audit/audit-log.md
    summary: "P0→P1 gate: user Go (2026-09-12); renderer-first order agreed among 6 主要要素"
    tags: [gate, P0, P1, renderer]
    recall: on_demand
    linked: [project-state]

  - id: req-circle-monthly-report
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/requirements/circle-monthly-report/要求.md
    summary: "P1 要求 for the first doc-type (サークル月次報告書), grounded in charter §3/§5 and packages/circle-monthly-report/README.md"
    tags: [requirements, P1, circle-monthly-report]
    recall: on_demand
    linked: [project-state]

  - id: reqspec-circle-monthly-report
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/requirements/circle-monthly-report/要件.md
    summary: "P2 要件 — FR1-FR8 (envelope/type検証・derived計算・escape・latexmk・CLI) + NFR1-2 (10秒以内・決定性), open items for P3"
    tags: [requirements, P2, circle-monthly-report, renderer]
    recall: on_demand
    linked: [project-state, req-circle-monthly-report]

  - id: design-renderer
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/design/programs/renderer.md
    summary: "P3 basic design (H4) for renderer — module split, interfaces, error mapping, provisional template layout policy. Open items resolved at P4/P5 (static pydantic + type walker; tabularx without longtable)."
    tags: [design, P3, H4, renderer]
    recall: on_demand
    linked: [project-state, reqspec-circle-monthly-report, plan-render-and-cli]

  - id: plan-ir-validate-and-derive
    scope: project
    tier: knowledge
    granularity: thread
    path: .agents/plans/algorithms/ir-validate-and-derive.md
    summary: "P4 plan (H5) FR1–FR4 — envelope/type validation + derived values. Do + Check complete 2026-09-12 (pass); AC1–5 covered by tests/test_ir_*.py"
    tags: [plan, P4, P5, H5, ir-schema]
    recall: on_demand
    linked: [design-renderer, project-state]

  - id: plan-render-and-cli
    scope: project
    tier: knowledge
    granularity: thread
    path: .agents/plans/programs/render-and-cli.md
    summary: "P4 plan (H4) FR5–FR8, NFR1–2 — escape, template, latexmk, CLI render. Do + Check complete 2026-09-12 (pass; W1 job name, W2 missing template → Act)"
    tags: [plan, P4, P5, H4, renderer, cli]
    recall: on_demand
    linked: [design-renderer, project-state]

  - id: ep-renderer-implementation
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-renderer-implementation.md
    summary: "plan-record + implementation of renderer (roadmap step 1). Module map, decisions, risks (branch name vs AGENTS.md, no Docker locally)"
    tags: [renderer, P5, implementation]
    recall: on_demand
    linked: [plan-render-and-cli, plan-ir-validate-and-derive]

  - id: review-renderer-cli-p6
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/reviews/2026-09-12-renderer-cli-p6-review.md
    summary: "P6 review of PR #8 (renderer/CLI): RTM FR1–8/NFR1–2 all pass; W1 latexmk job name from file stem, W2 missing template → exit 2; S1–S4 suggestions"
    tags: [review, P6, renderer, cli]
    recall: on_demand
    linked: [plan-render-and-cli, plan-ir-validate-and-derive, design-renderer]

  - id: design-api
    scope: project
    tier: knowledge
    granularity: thread
    path: docs/design/programs/api.md
    summary: "P3 design (H4, draft) for roadmap step 2 — entex.pipeline shared by CLI/API, POST /v1/render → PDF, RFC 9457 Problem Details, sync def + semaphore, P4 split pipeline-and-cli / api-render"
    tags: [design, P3, H4, api, pipeline]
    recall: on_demand
    linked: [design-renderer, review-renderer-cli-p6, project-state]

  - id: ep-p6-review-and-api-design
    scope: project
    tier: episodes
    granularity: thread
    path: .agents/memory/episodes/2026-09-12-p6-review-and-api-design.md
    summary: "P6 review pass + API P3 design on branch feature/npc; W1/W2 deferred into pipeline plan; branch name vs AGENTS.md rule"
    tags: [review, api, P6, P3]
    recall: on_demand
    linked: [review-renderer-cli-p6, design-api]

  # Optional pointer to global knowledge (no duplicate body):
  # - id: link-global-lessons
  #   scope: project
  #   tier: index
  #   granularity: archive
  #   path: null
  #   summary: "See global index: lesson-powershell-heredoc"
  #   tags: [shell]
  #   recall: on_demand
  #   linked: [lesson-powershell-heredoc]
