# Episodes

Per-turn work logs. **L1 minimum write**: one episode file per non-lightweight turn.

Required by memory bootstrap contract. Created empty at bootstrap; filled by `pm-turn-end` / `memory-record`.
