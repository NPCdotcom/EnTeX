# Secretary reference — context gather checklist

**After `memory-reference` Phase 0 (nav.yaml).**

1. **context-guard** report — pressure · loop2_required · reanchor
2. **nav.yaml** summary — session_intent · next_actions · alignment (from recall report)
3. Rule **`cursor-tooling-pm`** + `.agents/docs/CURSOR_AGENT_TOOLING.md`
4. **`cursor-tool-select`**
5. **[Loop2 only]** **`lifecycle-reference`** inputs（P/H/gate 再判定 · nav-brief 前）
6. Root **AGENTS.md**
7. `docs/requirements/` slugs
8. `.agents/plans/README.md` · `nav.active_plan` if set
9. `.agents/memory/episodes/` — latest 2（index 経由）
10. User message + this turn only
11. **`cursor-capabilities-reference`**

**Loop1**: skip step 5 — P/H/gate は **nav.alignment 引用のみ**（再推論禁止）。

Do not modify files.
