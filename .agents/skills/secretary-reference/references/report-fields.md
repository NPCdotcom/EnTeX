# Secretary reference — report fields

Pass to `secretary-brief`:

| Field | Content |
|-------|---------|
| **工程** | P0–P6 |
| **スコープ H** | H0–H5 focus |
| Active plan | path, `scope_level`, status |
| **Gate hint** | pass / block + forbidden（**Subagent 含む**） |
| **Cursor tools** | WebSearch: P0–P4 必須; browser MCP: yes/no; Shell: yes |
| **用語調査** | P0–P4 → required / n/a |
| Prior handoff | path or none |

Next: `secretary-brief` + `secretary-route` → PM → **`role-execute`**（Subagent 禁止）
