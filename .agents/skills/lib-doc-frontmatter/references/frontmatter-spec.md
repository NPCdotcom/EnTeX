# Frontmatter spec

- `status: agreed` only after **user** explicit confirm.
- Keep Facts / Assumptions / Open mutually exclusive.
- Plans need `related_design`, `scope_level`, Acceptance criteria linking to requirements/design.
