# Patch reference — eligibility

## Gather（最小）

1. **AGENTS.md** — source roots, principles
2. **Git** — `git status`, `git diff`, `git log -5 --oneline` on touched paths
3. **Plan**（if obvious）— `plan-reference` for **one** plan: related criterion title only
4. **Design** — behavior dispute only: `design-reference` path + Fact line
5. **rules/local/** — implement lens filename

## Verdict table

| Verdict | Next |
|---------|------|
| **patch_ok** | `patch-conduct` |
| **too_large** | **builder** |
| **no_spec** | **spec_designer** |
| **plan_gap** | **plan_slicer** |
| **unclear** | List questions; do not code |

Do not edit source here.
