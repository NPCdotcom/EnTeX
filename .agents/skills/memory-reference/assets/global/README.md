# Global memory bootstrap

Copy `index.yaml.example` to your Cursor user home:

```
%USERPROFILE%\.agents\memory\global\index.yaml
```

Create knowledge subdirs: `philosophy/`, `lessons/`, `preferences/`.

See `docs/MEMORY_ARCHITECTURE.md` and `references/global-paths.md`.
