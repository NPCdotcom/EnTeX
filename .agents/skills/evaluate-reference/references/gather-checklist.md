# Evaluate reference — gather checklist

**Role**: `evaluator` · **P6 Act** · Read-only.

1. `docs/project-state.yaml`, `docs/PROJECT_LIFECYCLE.md`（phase / H focus）
2. `.agents/plans/README.md` — `programs/` vs `algorithms/` vs roadmap folders
3. `docs/requirements/` slugs, `docs/design/` hierarchy
4. `docs/README.md` if present
5. Target plan or all plans + design index
6. `git log -20 --oneline`, `git status`
7. Top-level source dirs per **AGENTS.md**

Next: **plan-evaluate** or **project-evaluate**. Do not conduct evaluation here.
