{
  "name": "EnTeX dev (single image)",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "install": "pip install -e '.[dev]'",
  "ports": [
    { "name": "api", "port": 8000 }
  ]
}
