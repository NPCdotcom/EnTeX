# EnTeX project state. Update on gate pass / phase change.
# Session navigation (every turn): .agents/memory/state/nav.yaml — see .agents/docs/PM_ROUTING.md
# Gate: .agents/docs/ROLES_AND_GOVERNANCE.md · Lifecycle: docs/PROJECT_LIFECYCLE.md

current_phase: P1   # P0|P1|P2|P3|P4|P5|P6
phase_note: "P0ゲート Go 済み（2026-09-12、ユーザー承認）。着手順（charter §11）はrenderer優先で確認済み: ir-schema型語彙とdoc-package(circle-monthly-report)のscaffoldは済み、renderer本体（pydanticローダー→envelope→expr→escape→template.tex.j2→latexmk）が次。実装(implement-conduct)に進む前にP1要求→P2要件→P3基本設計→P4 planを通す。"

scope_focus:      # which H level we are clarifying now
  level: H1       # H0|H1|H2|H3|H4|H5
  path: "docs/requirements/circle-monthly-report/"

active_pdca:      # primary S1 cycle (program or algorithm) — none until a plan exists
  plan_path: ""
  scope_level: ""
  pdca_class: ""

# Stage-gate status (Gate Keeper = user)
gate_status:
  current: passed         # pending | passed | conditional | recycled | hold | killed
  proposal: "P1→P2: サークル月次報告書の要求(要求.md)から検証可能な要件(要件.md)へ"
  last_decision: "Go"
  last_decided_at: "2026-09-12"
  conditions: []
  gate_keeper: user       # fixed; PM proposes, user decides

last_gate_passed: "P0→P1 (2026-09-12)"
allowed_actions:
  - design-deliberate
  - design-record
  # - plan-record          # uncomment when P3+H4/H5 ready
  # - implement-conduct    # uncomment when plan agreed

open_blockers: []

# Adaptive lifecycle (skill: adaptive-lifecycle-plan)
lifecycle_plan:
  profile: "standard"      # hotfix | spike | standard | full | doc_only | custom
  brownfield: false
  last_planned_at: "2026-09-12"
  executed_phases: [P0, P1]
  skipped_phases: []
  skipped_stages: []
  recommended_cursor_mode: "Plan→Agent"
  requirements_depth: "standard"
  unit_scope: ""           # first candidate: roadmap step 1 — CLI JSON → PDF for one doc-type
  notes: "Roadmap fixed in charter §11: 1 CLI → 2 API → 3 second doc-type (gate) → 4 UI → 5 CSV batch."

# Optional — Linear
linear_sync:
  issue_state: ""
  issue_id: ""
  map_doc: docs/LINEAR_PHASE_MAP.md
