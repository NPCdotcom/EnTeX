# EnTeX project state. Update on gate pass / phase change.
# Session navigation (every turn): .agents/memory/state/nav.yaml — see .agents/docs/PM_ROUTING.md
# Gate: .agents/docs/ROLES_AND_GOVERNANCE.md · Lifecycle: docs/PROJECT_LIFECYCLE.md

current_phase: P6   # P0|P1|P2|P3|P4|P5|P6
phase_note: "P0ゲート Go 済み（2026-09-12）。要求(P1)→要件(P2)→renderer基本設計(P3)→plan-record(P4: algorithms/ir-validate-and-derive, programs/render-and-cli)→実装(P5、PR #8 を main へマージ)→P6 Check pass（docs/reviews/2026-09-12-renderer-cli-p6-review.md）。着手順1（CLI で1本通す）完了。Act 候補: W1/W2 小パッチ・renderer.md IF 追従。次は着手順2（API化）の P3 設計（docs/design/systems/api.md、draft）。"

scope_focus:      # which H level we are clarifying now
  level: H4       # H0|H1|H2|H3|H4|H5
  path: "docs/design/programs/renderer.md"

active_pdca:      # primary S1 cycle (program or algorithm)
  plan_path: ".agents/plans/programs/render-and-cli.md"
  scope_level: program
  pdca_class: S1

# Stage-gate status (Gate Keeper = user)
gate_status:
  current: pending        # pending | passed | conditional | recycled | hold | killed
  proposal: "P6 Act → 着手順2 の P3 ゲート: (1) レビュー W1（latexmk ジョブ名の正規化）/ W2（template 欠落を PackageError に）を小パッチで入れる (2) renderer.md IF 表を実装に追従 (3) docs/design/systems/api.md（API化 + 共通パイプライン関数）を agreed にして P4 plan-record へ。ユーザーの Go 待ち"
  last_decision: "Go"
  last_decided_at: "2026-09-12"
  conditions: []
  gate_keeper: user       # fixed; PM proposes, user decides

last_gate_passed: "P5→P6 (2026-09-12, review-conduct pass on PR #8; user accepted 推奨案 A→B on 2026-09-12)"
allowed_actions:
  - design-deliberate
  - design-record
  - patch-conduct        # レビュー W1 / W2 の小パッチ
  - plan-record          # api.md が agreed になった後
  - review-conduct

open_blockers: []

# Adaptive lifecycle (skill: adaptive-lifecycle-plan)
lifecycle_plan:
  profile: "standard"      # hotfix | spike | standard | full | doc_only | custom
  brownfield: false
  last_planned_at: "2026-09-12"
  executed_phases: [P0, P1, P2, P3, P4, P5, P6]
  skipped_phases: []
  skipped_stages: []
  recommended_cursor_mode: "Plan→Agent"
  requirements_depth: "standard"
  unit_scope: ""           # first candidate: roadmap step 1 — CLI JSON → PDF for one doc-type
  notes: "Roadmap fixed in charter §11: 1 CLI → 2 API → 3 second doc-type (gate) → 4 UI → 5 CSV batch."

# Optional — Linear
linear_sync:
  issue_state: ""
  issue_id: ""
  map_doc: docs/LINEAR_PHASE_MAP.md
