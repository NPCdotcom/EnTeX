# EnTeX project state. Update on gate pass / phase change.
# Session navigation (every turn): .agents/memory/state/nav.yaml — see .agents/docs/PM_ROUTING.md
# Gate: .agents/docs/ROLES_AND_GOVERNANCE.md · Lifecycle: docs/PROJECT_LIFECYCLE.md

current_phase: P6   # P0|P1|P2|P3|P4|P5|P6
phase_note: "着手順1（CLI で1本通す）は P6 Check pass で完了（docs/reviews/2026-09-12-renderer-cli-p6-review.md）。着手順2（API化）: P3 設計 docs/design/programs/api.md agreed → P4 plan 2 本（programs/pipeline-and-cli → programs/api-render）→ P5 Do 完了 → P6 Check pass（2026-09-12、docs/reviews/2026-09-12-api-and-pipeline-p6-review.md: Critical 0 / Warning 1 / Suggestion 6、217 tests、CI green）。pipeline-and-cli は PR #10 で main にマージ済み、api-render は PR #12（ready for review、マージはユーザー）。Warning W1（chunked 本文の 413 を stream 化）は公開配置（認証 plan）の前に入れる小パッチ。次は着手順3（2 文書種目 = charter §6 の合否判定）の P3 設計。\n【2026-09-12追記・issue #11、main で先行反映済み】最初の文書種の題材を「サークル月次報告書」から「サークル部会ログのフォーマット化」に差し替え（charter §5/§11、ADR-0001）。月次報告書は運用自体が存在しないと判明したため。renderer/pipeline/CLI/API本体（H4）は文書種非依存の設計のため作り直し不要。影響は docs/requirements/circle-monthly-report/・packages/circle-monthly-report/ に閉じる（H1、要P1やり直し）。着手順2（API化、H4）の current_phase / scope_focus はこの差し替えの影響を受けない。具体的な様式はユーザー提示情報を待って着手する。"

scope_focus:      # which H level we are clarifying now
  level: H4       # H0|H1|H2|H3|H4|H5
  path: "docs/design/programs/api.md"

active_pdca:      # primary S1 cycle (program or algorithm)
  plan_path: ".agents/plans/programs/api-render.md"   # pipeline-and-cli と合わせて Check
  scope_level: program
  pdca_class: S1

# Stage-gate status (Gate Keeper = user)
gate_status:
  current: passed         # pending | passed | conditional | recycled | hold | killed
  proposal: "P6 Check pass（2 plan 合同）。(1) PR #12 を main へ（ユーザー）。(2) 着手順3（2 文書種目）の P3 設計へ進む — charter §6 の合否判定『src/entex/ を触らずに文書種を足せるか』を /v1/doc-types で確認する。W1 / S1〜S6 はブロッカーではなく、認証 plan または着手順3 のついでに拾う"
  last_decision: "Go"
  last_decided_at: "2026-09-12"
  conditions: []
  gate_keeper: user       # fixed; PM proposes, user decides

last_gate_passed: "P3→P4 (2026-09-12, user: api.md agreed + Go on the 2-plan split + pydantic>=2.9 approved). P6 Check pass for both plans recorded 2026-09-12 (agent verdict; merge of PR #12 is the user's call)"
allowed_actions:
  - design-deliberate    # 着手順3（2 文書種目）の P3
  - design-record
  - plan-record
  - patch-conduct        # W1 / S1 の小パッチ
  - review-record

open_blockers:
  - "部会ログの具体的な様式（頻度・欄構成・行数の上限など）待ち。ユーザー提示後にdocs/requirements/配下でP1要求定義をやり直す（issue #11・ADR-0001）"

# Adaptive lifecycle (skill: adaptive-lifecycle-plan)
lifecycle_plan:
  profile: "standard"      # hotfix | spike | standard | full | doc_only | custom
  brownfield: false
  last_planned_at: "2026-09-12"
  executed_phases: [P0, P1, P2, P3, P4, P5, P6]
  skipped_phases: []
  skipped_stages: []
  recommended_cursor_mode: "Plan→Agent"
  requirements_depth: "standard"
  unit_scope: ""           # first candidate: roadmap step 1 — CLI JSON → PDF for one doc-type
  notes: "Roadmap fixed in charter §11: 1 CLI → 2 API → 3 second doc-type (gate) → 4 UI → 5 CSV batch."

# Optional — Linear
linear_sync:
  issue_state: ""
  issue_id: ""
  map_doc: docs/LINEAR_PHASE_MAP.md
