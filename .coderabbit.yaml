# CodeRabbit設定
# https://docs.coderabbit.ai/reference/configuration
#
# 注意: スター数の少ないリポジトリは自動レビューが走らないことがある。
# PRを出すたび・修正をpushするたびに `@coderabbitai review` をコメントする(AGENTS.md)。

language: ja-JP

reviews:
  poem: true
  path_filters:
    - "!out/**"
    - "!**/*.pdf"
    - "!.agents/**"
